[providers]
python = "3"

[phases.setup]
nixPkgs = ["pkgs.nodejs_20", "pkgs.python3", "pkgs.gnumake", "pkgs.gcc"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"

